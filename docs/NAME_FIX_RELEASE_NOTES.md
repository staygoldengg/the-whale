# Name Fix Release Notes

This package standardizes the app identity everywhere as:

**The Whale — Westhampton Day School AI Companion**

Updated areas:

- `package.json` name set to `the-whale`
- Electron product name set to `The Whale`
- Android/Capacitor app ID set to `com.westhampton.thewhale`
- PWA manifest name set to `The Whale — Westhampton Day School AI Companion`
- Browser metadata title standardized
- `.env.naming.example` added with `WHALE_` custom app env naming
- `docs/BRANDING_STANDARD.md` added
- README heading standardized
- Home page subtitle standardized

Note: Typecheck requires dependencies to be installed with `npm install` first. The source package itself has been updated without bundling `node_modules`.
